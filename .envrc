# .envrc for c360-lake project

# Requires use_nvm() to be defined in ~/.config/direnv/direnvrc
#   see: https://github.com/direnv/direnv/wiki/Node#using-nvm
if declare -Ff use_nvm >/dev/null; then
  use nvm v10.16.0
fi
